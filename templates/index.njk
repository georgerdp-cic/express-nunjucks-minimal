{% extends "_layouts/default.njk" %}

{% block body %}

    <div style="color: red;" > Text inside the block </div>
    
    {% for feature in featList %}
        <div> {{ feature.name }} - {{feature.desc}} </div>
    {% endfor %}

    {% include "_partials/includesexample.njk" %}

{% endblock %}